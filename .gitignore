# Dependencias
node_modules/

# Next.js
.next/
out/
build/

# Base de datos (runtime, se regenera con `bun run setup`)
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm

# Entorno y secretos — NUNCA commitear
.env
.env.*
!.env.example

# Logs
*.log

# Sistema
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Datos en caliente de los mini-servicios
mini-services/stream-service/data/

# Artefactos locales de desarrollo (capturas, búsquedas, notas)
scripts/shot_*.png
scripts/*_search.json
scripts/tsc-check.log
scripts/vlm-*.json
scripts/.run/

# Playwright (FASE 22): artefactos y reportes — nunca al repo
test-results/
playwright-report/
blob-report/

# Entorno de desarrollo sandbox (no forman parte del proyecto)
/skills/
/.zscripts/
/download/
/examples/
/Caddyfile
/upload/
/worklog.md
tests/python-*.sh
tests/database-*.sh
tool-results/

# Datos en caliente del stream-service cuando corre desde la raíz (NMS store)
/data/
.install-test/

# Staging de empaquetado del installer oficial (bundle-server.ts / appimage.sh)
dist/
installer/gui/src-tauri/binaries/
installer/gui/src-tauri/resources/
installer/gui/src-tauri/gen/
installer/gui/src-tauri/target/

# Licenciamiento — artefactos del generador (NUNCA commitear claves ni
# licencias emitidas: la clave privada vive FUERA del repo)
tools/license-generator/out/
tools/license-generator/history/
tools/license-generator/*.key
tools/license-generator/keys/
data/licensing/

# Tests de integración de licencias (sandbox: TMPDIR relativo crea ./tmp)
/tmp/
backups/*.db
