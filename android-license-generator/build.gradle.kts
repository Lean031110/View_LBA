// Proyecto raíz — ViewLBA License Generator (app privada del administrador).
// Versiones EXACTAS (reproducibles): ver gradle/libs.versions.toml-free setup
// aquí mismo para evitar indirección.
plugins {
    id("com.android.application") version "8.7.3" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
