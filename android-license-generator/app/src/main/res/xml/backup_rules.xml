<?xml version="1.0" encoding="utf-8"?>
<!-- NUNCA respaldar nada (la DB contiene las claves privadas del emisor;
     el flujo de respaldo oficial es el .vlbak cifrado con contraseña). -->
<full-backup-content>
    <exclude domain="root" path="." />
    <exclude domain="database" path="." />
    <exclude domain="sharedpref" path="." />
    <exclude domain="file" path="." />
</full-backup-content>
